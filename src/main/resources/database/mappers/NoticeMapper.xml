<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.example.demo.notice.NoticeRepository">
  	<select id="getList" parameterType="Pager" resultType="NoticeVO">
  		select * from destudynotice
  		where
  			<choose>
  				<when test="kind == 'writer'">writer</when>
  				<when test="kind == 'contents'">contents</when>
  				<otherwise>title</otherwise>
  			</choose>
  		like concat('%', #{search}, '%')
  		order by num desc
  		limit #{startRow}, #{perPage}
  	</select>
  	
  	<select id="getTotalCount" parameterType="Pager" resultType="Long">
  		select count(num) from notice
  		where
  			<choose>
  				<when test="kind == 'writer'">writer</when>
  				<when test="kind == 'contents'">contents</when>
  				<otherwise>title</otherwise>
  			</choose>
  		like concat('%', #{search}, '%')
  	</select>
  	
  	<select id="getSelect" parameterType="NoticeVO" resultType="NoticeVO">
  		select * from destudynotice where num=#{num}
  	</select>
  	
  	 <update id="pageView">
  		update destudynotice set hit = hit + 1
  		where id = #{id}
  	</update>
  	
  	<insert id="setInsert" parameterType="NoticeVO" useGeneratedKeys="true" keyProperty="num">
  		insert into destudynotice (num, title, contents, writer, hit, date)
  		values (#{num}, #{title}, #{contents}, #{writer}, 0, now())
  	</insert>
  	
  	<update id="setUpdate" parameterType="NoticeVO">
  		update destudynotice set title=#{title}, contents=#{contents}
  		where num=#{num}
  	</update>
  	
  	<delete id="setDelete" parameterType="NoticeVO">
  		delete from destudynotice where num=#{num}
  	</delete>
  	
  	<insert id="setReplyInsert" parameterType="NoticeVO" useGeneratedKeys="true" keyProperty="num">
		insert into destudynotice (num, title, contents, writer, hit, date, ref, step, depth)
		values (null, #{title}, #{contents}, #{writer}, 0, now(),
		(select R.ref from (select * from destudynotice where num=#{num}) R),
		(select S.step+1 from (select * from destudynotice where num=#{num}) S),
		(select D.depth+1 from (select * from destudynotice where num=#{num}) D)
		)
  	</insert>
  	
  	<update id="setReplyUpdate" parameterType="NoticeVO">
  		update destudynotice set step=step+1
  		where ref=(select R.ref from (select ref from destudynotice where num=#{num}) R)
  		and
  		step > (select S.step from (select step from destudynotice where num=#{num}) S)
  	</update>
  	
  	<update id="setRefUpdate" parameterType="NoticeVO">
  		update destudynotice set ref=#{num} where num=#{num}
  	</update>
  </mapper>
  
